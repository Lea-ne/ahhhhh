<?php

/**
 * @file
 * Paragraph Bundle Image.
 *
 * Filename:     paragraph_bundle_image.module
 * Website:      https://www.flashwebcenter.com
 * Description:  template.
 * Developer:    Alaa Haddad https://www.alaahaddad.com.
 */

use Drupal\Core\Routing\RouteMatchInterface;


/**
 * Implements hook_theme for the paragraph().
 */
function card_simple_article_theme($existing, $type, $theme, $path) {
  return [
    'paragraph__card_simple_article' => [
      'base hook' => 'paragraph',
    ],

    'node__card_simple_article' => [
      'base hook' => 'node',
    ],
  ];
}






function card_simple_article_entity_view(array &$build, \Drupal\Core\Entity\EntityInterface $entity, $view_mode, $langcode) {
  if ($entity->getEntityTypeId() === 'paragraph' && $entity->bundle() === 'card_simple_article') {
    $build['#attached']['library'][] = 'card_simple_article/component_style';
  }
}